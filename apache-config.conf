<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
```

## 6. Desplegar

Una vez configurado todo, haz clic en **"Create Web Service"**. Render automáticamente:
- Clonará tu repositorio
- Ejecutará el build
- Desplegará tu aplicación
- Te dará una URL como: `https://tu-proyecto.onrender.com`

## Consejos adicionales:

- **Nunca subas credenciales a GitHub**: Usa siempre variables de entorno
- **`.gitignore`**: Asegúrate de incluir:
```